#!/usr/bin/env bash
# brain-handoff-next — canonical wrapper for handoff_next.sh
HDIR="${BRAIN_DIR:-$HOME/gmgn-brain}/scripts/lib/handoff-system"
if [[ "handoff_next.sh" == *.py ]]; then
  exec python3 "$HDIR/handoff_next.sh" "$@"
else
  exec bash "$HDIR/handoff_next.sh" "$@"
fi
