#!/usr/bin/env bash
# codex-handoff — Create, inspect, and manage Codex↔Claude handoff packets in the handoff inbox
set -euo pipefail
exec python3 "${BRAIN_DIR:-$HOME/brain}/scripts/lib/codex/codex_handoff.py" "$@"
